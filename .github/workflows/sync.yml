name: 同步 Notion 資料到班網

on:
  workflow_dispatch:        # 老師手動按「Run workflow」立即更新
  repository_dispatch:      # iOS 捷徑／API 一鍵觸發（event_type: sync-now）
    types: [sync-now]
  schedule:
    # 台灣時間 07:00 / 16:00 / 20:00（UTC+8）
    - cron: "0 23 * * *"
    - cron: "0 8 * * *"
    - cron: "0 12 * * *"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 同步 Notion 資料
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
        run: node scripts/sync-notion.mjs

      # 崑山校網是 TANet 主機，對 GitHub（Azure）機房 IP 不一定通得到。
      # 這一步失敗只代表「學校公告這次沒更新」，不該連累聯絡簿／公告／週報整批不上站，
      # 因此設 continue-on-error：步驟本身仍會在 Actions 上標紅，方便老師看到。
      - name: 同步校網公告（併入學校公告分頁；須在 sync-notion 之後）
        continue-on-error: true
        run: node scripts/sync-school-news.mjs

      - name: 同步 Google 日曆
        run: node scripts/sync-gcal.mjs

      - name: 同步 Drive 相簿
        env:
          DRIVE_API_KEY: ${{ secrets.DRIVE_API_KEY }}
        run: node scripts/sync-drive.mjs

      - name: 提交更新
        run: |
          git config user.name "class-website-bot"
          git config user.email "bot@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "資料沒有變化，不需要更新"
          else
            git commit -m "同步班網資料 $(TZ=Asia/Taipei date '+%Y-%m-%d %H:%M')"
            git push
          fi
