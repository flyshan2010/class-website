# scripts/classos 腳本索引（用途・狀態・可否重跑）

> 建立 2026-09-12。**動手前先看這張表**：Phase F 選單裡有 20 支腳本，其中一半是早就跑完的一次性任務，
> 選錯不會報錯、只會安靜改壞資料（2026-09-12 差點發生：要補第1週獎懲時，f23 看起來「就是那支」，
> 但它 `date >= 2026-08-28` 沒有上限、還會改寫來源列的週次——今天跑會污染第2、3 週）。
>
> 狀態標記：**常用**＝還會再跑／**一次性**＝任務已完成，不該再跑／**驗收用**＝證明護欄有效／**升級用**＝2027-06 才用。
>
> **2026-09-12 起這些狀態直接寫在 Phase F 選單上**（例：`[一次性・已完成・會蓋掉 Notion 最新進度] f20-import-daily-plan`），
> workflow 會從選項字串抽出 `fNN-xxx` 當檔名。新增腳本時**選單與本表一起加**，
> 沒有狀態前綴的選項視為漏標（健檢可比對）。

| 腳本 | 做什麼 | 狀態 | 可重跑？ |
|---|---|---|---|
| `f24-weekly-settle-preview.mjs` | 週五週結**試算**（七項＋通膨體檢＋共同目標啟用條件）。**永不寫帳本**，只在收件匣留待審 | **常用**（每週五自動） | ✅ 冪等，同週會刷新同一筆待審 |
| `f29-settle-week-rewards.mjs` | 補結某一週的**獎懲入帳**（只建帳本列，不動來源列）。鍵＝紀錄id×學生id | **常用**（發現漏發時） | ✅ 冪等，再跑是 0 筆 |
| `f31-settle-week-salary.mjs` | 週結**薪水類入帳**（①職務 ③打掃 ④午餐 ⑤常規獎勵）。**純寫入器，一行公式都沒有**：金額由 f24 的「💰 薪水類逐人明細」以 `payload` 傳入。鍵＝事由前綴×學生頁id | **常用**（每週五發布） | ✅ 冪等，逐人逐類跳過已入帳，可補漏 |
| `f32-publish-week-reports.mjs` | 某一週**學習報告發布前機器檢查＋勾發布**（份數／必填欄／學年／姓名對名冊／學生檔案 relation）。**任一項不過 exit 1 整批不勾**；等第乙以下只列名單交人工 | **常用**（每週五發布） | ✅ 冪等，已勾的不重勾 |
| `f33-price-draft.mjs` | 浮動調價**達標就寫草稿**（SPEC_班級經濟機制 §3-3、§4）：每週五接 f24 後檢查（判斷在 `lib/price.mjs` evaluate：近 4 週中位數÷上次定價 W，±15% 連續 2 週、冷卻 4 週、10/31 前只記錄、6 月不生效），每週收件匣一列「調價監測」，達標才寫「下期價格／調價生效日」＋預告公告 |
| `f34-price-apply.mjs` | 浮動調價**生效日套用**：生效日到＋未否決 → 改價並清空兩欄；否決 → 清空＋撤公告；下期價格空白 → 不改價 | 自動（sync.yml 同步前） | ✅ 冪等，已套用的欄位已清空 |
| `f35-cm-events.mjs` | **R18 課堂事件包入庫**（SPEC_R18事件包入庫腳本）：規則在 `lib/cm-events.mjs`（sim `sim/cm-events.mjs` 86 題）；MODE＝dry-run／compare（對照 routine 已寫的列，有差異才建待審；FROM／TO 可回溯指定事件日期）／execute（2026-09-25 起正式執行）；`SANDBOX=1` 一次性沙盒實跑 | `cm-events.yml`：2026-09-25 起平日每小時 **execute**；週五 `weekly-settle.yml` 先跑一次；手動 dry-run／execute／compare／sandbox | ✅ 冪等（事件id 防重複） |
| `f36-fix-r18-misseat.mjs` | 一次性：更正 routine 把 9/16 座號15 潔牙兩筆掛到座號16（紀錄庫改回＋帳本退回／補扣＋常規獎勵差額，驗證掛錯才寫） | `classos-phase-f.yml` 手動 | ✅ 2026-09-25 已執行，別再跑（重跑會因「不是掛座號16」擋下） |
| `f03-backfill-year.mjs` | 回填「學年」。**同步失敗訊息就是叫人跑這支** | **常用**（護欄指定修復工具） | ✅ |
| `f27-prune-dangling-relations.mjs` | 清死關聯：**只做 👥 學生名冊**，可 `seat=N` 單人試跑 | 常用（症狀出現時） | ✅ |
| `f28-prune-dangling-relations-all.mjs` | 清死關聯：**全庫版**（27 個庫，含名冊） | 常用（症狀出現時） | ✅ |
| `f11-upgrade-wizard.mjs` | 學年升級精靈（只寫名冊與網站設定） | **升級用**（2027-06） | ⚠️ 先用 f10 沙盒演練 |
| `f10-sandbox.mjs` | 沙盒建置／拆除（切換式） | 升級用 | ✅ |
| `f05-test-failloud.mjs` | 實測「學年為空 → 同步中止」真的會觸發 | 驗收用 | ✅ |
| `f12-simulate-skill-writes.mjs` | 9 支 skill 的學年寫入模擬 | 驗收用 | ✅ |
| `f23-settle-week1.mjs` | 開學補結（8/28 返校日＋8/31 開學日的獎懲）**並把來源列週次正規化成第1週** | **已廢止 2026-09-12**（老師裁示）：選單已移除，腳本開頭有硬擋（要跑得設 `ALLOW_DEPRECATED_F23=yes`） | ❌ **不要再跑**：date 無上限＋會改寫來源列週次 |
| `f01-purge-test-data.mjs` | 清 Phase A 驗收測試資料 | 一次性（2026-07-25 已執行） | ❌ |
| `f02-add-year-field.mjs` | 11 個庫新增「學年」欄 | 一次性（欄位已存在） | ❌ |
| `f04-unpublish-mock-reports.mjs` | 下架模擬學習報告 | 一次性（已執行） | ❌ |
| `f13-fill-contactbook.mjs` | 每日作業進度表 → 聯絡簿批次寫入 | 一次性（開學前已執行） | ❌ 會覆寫老師後來的修改 |
| `f14-purge-mock-data.mjs` | 清除全流程演練遺留的模擬資料 | 一次性（2026-08-01 已執行） | ❌ |
| `f20-import-daily-plan.mjs` | xlsx → Notion「📅 每日課程進度」遷移 | 一次性（已遷移，xlsx 已封存） | ❌ 會蓋掉 Notion 上的最新進度 |
| `f21-relink-daily-plan.mjs` | 依進度文字重掛「◯◯單元」relation | 半常用（大批調課後） | ✅ 但先 dry-run 看對照 |
| `f22-renumber-review-rounds.mjs` | 複習輪次編號對齊課次代碼 | 一次性（2026-08-29 已執行） | ❌ |
| `f25-clear-weekly-mock.mjs` | 清週報罐頭素材（活動／亮點／家長配合） | 一次性（2026-09 已執行） | ❌ |
| `f26-clear-weekly-focus.mjs` | 清週報預寫「學習重點」 | 一次性（2026-09 已執行） | ❌ |
| `audit-year-rules.mjs` | 稽核各處學年規則是否一致 | 驗收用 | ✅ |
| `sim/` | 週結模擬驗證與週五發布乾跑（**完全不寫入**，api 換成拋錯） | 常用（施工後驗收） | ✅ |

## 為什麼「算」與「寫」要分成不同腳本（f24 vs f29／f31）

**刻意分開：一支只算、一支只寫。** f24 每週五自動跑、任何人都可以看；f29／f31 會真的動錢，
必須手動選 task＋mode=execute 兩道才跑得起來。合併會讓「試算」與「入帳」共用一個入口，
等於把最後那道人工閘門拿掉——這是把關三分類的底線，不是重複程式碼。

**f31 更嚴格一點：它連公式都不放。** 逐人金額只有 f24 算得出來，f31 只是照著 payload 建帳列。
若把公式抄一份進 f31，入帳端就會變成第二份會自己漂掉的正本（U58）——兩邊各改各的，
某週開始金額對不上時沒人知道該信哪一份。

## 共用的三條寫入規矩（新腳本照抄）

1. **預設 dry-run**：`isExecute()` 為假就只印不寫；Actions 的 mode 預設 dry-run。
2. **防重複鍵要含「對象」**：一筆來源可掛多位學生，鍵只用來源 id 會讓第二位以後被靜默漏掉（U41）。
3. **execute 後一定回讀驗收**：比對筆數與防重複鍵，不要相信自己的計數器——
   2026-09-12 f29 第一版把 `api()` 的回傳當成頁物件，53 筆明明寫進去了卻全報失敗，
   是回讀那一關把真相說出來的。
