# 🧳 收工交接（下次接續參考）

> 最後更新：2026-07-18。每次收工更新這份，下個 session 先讀這裡。
> 最新跨 agent 正本見 `Claude/SESSION_HANDOFF.md`（2026-07-18：兌換申請流＋Phase 3 完工）。

---

## 套件一：已完成功能盤點（對照設計藍圖五期）

| 期 | 內容 | 狀態 |
|---|---|---|
| Phase 1 | 資料中樞：名冊＋班經與學習紀錄庫＋/class-log＋報告自動生成 | ✅ 完成 |
| Phase 2 | 班級銀行＋商店＋小小銀行頁＋/class-bank | ✅ 完成 |
| Phase 2.5 | 真實 27 人名冊＋學期成績庫＋/class-grades | ✅ 完成 |
| 額外 | /class-counsel 學生輔導、班級管理工作台（課堂即時層）、報告級距圖表 | ✅ 完成 |
| 額外 | **學生學習報告單頁化列印（A4 橫式，2026-07-13）** | ✅ 完成 |
| 額外 | **內容評量改版＋四等第統一特優/優/甲/乙/丙（2026-07-13）** | ✅ 完成 |
| Phase 4（起步） | **報告「理財表現」區塊**（存款/收入/支出/儲蓄率＋收支觀察，2026-07-13） | ✅ 完成 |
| 維運 | **名冊→對照表一鍵同步檢查腳本** sync-roster-map.mjs（2026-07-13） | ✅ 完成 |
| Phase 4 | **期末總報告功能**（五向度逐週成長曲線＋學期總評，skill class-term-report，2026-07-13） | ✅ 功能已建置＋部署 |
| Phase 5（部分） | 首頁「⚡ 立即更新班網」一鍵按鈕 | ✅ 完成 |
| 額外 | **班級銀行兌換申請流「能看也能買」（2026-07-18）**：🛒 兌換申請 DB＋代理 v2.1＋小小銀行兌換鈕＋教師專區核可後台 | ✅ 完成（待老師部署代理 v2.1） |
| Phase 3 | **教學五段流程整合（2026-07-18）**：🚀 教學單元 DB＋/lesson-flow skill 三件套＋班網教學駕駛艙頁（cockpit.html） | ✅ 完成＋上線 |

**尚未動工：**
- **Phase 4｜生活分析＋報告彙整強化（尚餘）**：
  - 生活 SEL 分析（PBS／阿德勒正向管教 → 生活向度評分＋建議、「SEL 觀察」區塊）
  - 期末總報告（整學期一鍵總評＋成長曲線圖）← 建議下一個做
  - 學期回顧頁：班級經濟大事記、全班 SEL 成長雷達（去識別化聚合）
  - ~~報告「理財表現」區塊~~ ✅ 2026-07-13 完成（在身分卡左欄，收支＋儲蓄率＋觀察）
  - 稅／保險／捐款等進階社會模擬（Phase 4 後評估）
- **Phase 5（其餘）**：粉蠟筆風全站強化、行動版 /class-log 語音速記捷徑、備份與查詢碼輪換提醒。

---

## 套件二：下一步建議（投報率排序）

1. 🥇 **Phase 4 兩個單點收尾**（地基已在、當週見效）：
   - **報告「理財表現」區塊**（銀行資料已有，報告加一塊，呼應金融教育主軸）← 最推薦先做
   - **期末總報告**（開學後第一個學期末就會用到，早做早驗證）
2. 🥈 **維運型修正**：
   - ~~名冊 → 對照表一鍵同步檢查~~ ✅ 已完成 `scripts/sync-roster-map.mjs`
   - 開學前清掉/重生兩個 `data/reports/1.json`、`30.json` 範本（見套件三注意事項）← 仍待辦
3. 🥉 **Phase 3（/lesson-flow）**：工程量最大，留給暑假中段完整時段。

> 時程：開學 2026-08-31（約 7 週後）。開學就會天天用的先做，Phase 3 大工程排暑假中段。

---

## 套件三：本次 session（2026-07-13）變更摘要

**A. 學生學習報告單頁化**
- `assets/css/style.css` 的 `@media print` 全面壓縮，並移除大區塊的 `break-inside:avoid`
  （原本它把級距圖整塊推到第 2 頁）。實測列印寬度(277mm)下總高 **165.8mm**，單頁（安全邊 ~9mm）。
- 若從 GitHub Pages 線上站列印，需 **部署後** 才會生效；本機測試已通過。

**B. 「考試成績」→「內容評量」＋資料來源重定義**
- 報告等第「考試成績」更名 **內容評量**：平常週＝紀錄庫「評量」類＋學期成績庫平時分；
  期考週＝三科平均（平時50%＋期考50%）套分數等第。
- 已改：`class-report/SKILL.md`、`class-log/SKILL.md`（新增「評量」類別）、
  `assets/js/report.js`（GRADE_COLORS 鍵）、`scripts/sync-notion.mjs`（相容讀 內容評量 ?? 考試成績）、
  `docs/學生評分標準.md`。
- **Notion 已改**：報告庫屬性「考試成績」→「內容評量」；紀錄庫「類別」新增「評量」。

**C. 四等第統一為 特優／優／甲／乙／丙（基準＝甲）**
- 修正原本混用：作業成績曾是 A+/A/B+/B/C、上課參與與生活常規曾是 優/良/可。
- **Notion 報告庫四個 select（內容評量／作業成績／上課參與／生活常規）已全部改為**
  特優:pink／優:red／甲:orange／乙:yellow／丙:gray。
- 無紀錄預設值由「乙」改為 **「甲」**。

**D. 無空白鐵則升級 v9**
- 中性語句改為「每週重新生成、換句話說」，生成前比對上週同欄，字面高度重複即重寫，杜絕罐頭句。

**E. 報告「理財表現」區塊（Phase 4 起步）**
- 身分卡左欄新增：🏦 存款／收入／支出／💰 儲蓄率＋一句收支觀察（金融教育）。
- 放在身分卡（利用雷達圖那側的空間）→ 不增加列印高度；實測考週單頁 169.7mm（可用175mm）。
- 資料：`scripts/sync-notion.mjs` 的 `bankFinanceBySeat()` 依帳本金額正負計 income/expense，
  儲蓄率＝結餘/收入；`report.js` 的 `financeNote()` 依儲蓄率給觀察語。
- **需下次 Notion 同步（有已發布報告＋帳本交易）才會顯示**；純範本或無帳的學生不顯示。

**F. 名冊對照表一鍵同步 `scripts/sync-roster-map.mjs`**
- `NOTION_TOKEN=xxx node scripts/sync-roster-map.mjs`：從 Notion 名冊重建對照表並印差異；`--dry` 只檢查。

**G. 期末總報告功能（Phase 4，2026-07-13 建置＋部署）**
- Notion 報告庫加欄位：`報告類型`(每週/期末總報告)、`學期總評`、`學期成長亮點`、`下學期目標`。
- 班網 report.js：報告類型＝期末總報告時，標題改「🌟 學期學習總報告」、概覽改畫**五向度逐週成長曲線**
  （折線圖，資料取自該生本學期各週報告雷達分數，免額外儲存）＋學期總評/成長亮點/下學期目標區塊。
- 新增 skill `class-term-report`（觸發「跑期末總報告」）：彙整整學期週報＋成績庫→產出期末總報告列。
- 已用 10 週＋期末的 mock 驗證：5 條成長曲線、50 資料點、各區塊正常。

**H. 同步上站方法（2026-07-13 全鏈路實測通過）**
- `sync.yml` 有 `workflow_dispatch`，Claude 可用 `gh workflow run sync.yml --ref main` 觸發同步
  （Action 帶 repo 的 NOTION_TOKEN secret 跑，**Claude 不需持有 token**）→ 資料即上班網。
- **一鍵更新按鈕已完整可用（本次實測）**：首頁「⚡ 老師專區 → 🔄 立即更新班網」→ 輸入更新口令
  → Apps Script proxy（已部署，URL 在 `data/site-config.json` updateProxyUrl）→ 送
  `repository_dispatch {event_type: sync-now}` → `sync.yml` 跑 → 約 2–3 分鐘後班網更新。
  - 實測：proxy 回應正常（錯誤口令回「口令錯誤」＝GH_TOKEN/PASSWORD 都設好）；
    以 `gh api .../dispatches -f event_type=sync-now` 模擬按鈕送出，成功觸發並完成同步。
  - **老師唯一要記得的是「更新口令」**（設在 Apps Script 專案設定→指令碼屬性 PASSWORD；
    忘記可到 script.google.com 重設）。
- 亦可等每日 3 次自動同步（台灣 07:00/16:00/20:00）。

**I. 班級商店去重（2026-07-13 完成）**
- 商店原 21 品項有 7 組重複（07-07 舊批＋07-12 新批各建一次）。已將 7 個舊重複列設「上架＝否」
  （非破壞，資料保留），保留較新／描述較佳者。**現上架 14 品項**（小物 5＋特權 9）。
- 造型鉛筆／香香鉛筆為不同商品，皆保留。已同步上站。

**真實全實跑（使用者要求「真實全實跑」）── 進行中，1–5 已含期末總報告**
- ✅ 座號 1–9 各 4 個「每週」報告列（W5/W10/W15/W20，五向度，發布=YES，title 前綴「（模擬）」）。
- ✅ **座號 1–5 已各建 1 份「期末總報告」列（2026-07-13）**：五向度學期代表值（末3週均）、期末四等第、
  各科學期總評與建議、學期總評、成長亮點、下學期目標、家長建議，均**依真實週報趨勢＋成績庫期末**撰寫
  （座號3 有進步獎+11.9、座號5「後段發力」）。已發布＋同步＋**在站上驗證成長曲線與版面 OK**。
  未含「考試成績摘要（級距）」——需全班期末彙整班平均/級距，留待做全班時一起補。
- ✅ **清掉座號 1、2 的舊批殘骸週報**（第一/十/二十週、帶日期區間、低分）→ 改設發布=否（非破壞，可復原），
  讓 1–5 一律乾淨 4 週序列（第五/十/十五/二十週）。座號 6–9 仍是稀疏週報、無期末總報告。
- ✅ **座號 1–5 每週報告補滿內容（2026-07-13 第二輪）**：原 W5–W20 是「只有五向度」的稀疏列，
  已另建 20 列「完整週報」（各科狀態/建議、四等第、學生亮點、短期/長期目標、家長建議，依當週水準撰寫），
  發布＋同步後站上每週分頁每欄都有內容（不再出現「本週無特別記錄」保底）。
  稀疏舊列仍在庫但被 sync 去重隱藏（見下）；要清時用 title 含「（模擬）」批次刪。
- ⬜ 待做（若要全班）：座號 6–27 期末總報告＋完整週報；全班考試成績摘要（級距）。
- 使用者決策點：**先看 1–5 效果 → 再決定要不要全班**。清除便利：所有模擬列 title 皆含「（模擬）」。

**⭐⭐ 第二輪修正（2026-07-13）：期末總報告單頁化＋週報同週去重＋每週補內容**
- **期末總報告列印壓單頁**：成長曲線 viewBox 由 560×250 改矮版 **560×165**（`report.js`），
  列印 `@media print` 再限高 **44mm**＋收緊 report-top/box 間距（`style.css`）。
  以「內容版週報 ~166mm 單頁」為基準推算，期末總報告 ~186–191mm，單頁可容（200mm）。
  ⚠️ **本 session 瀏覽器截圖工具持續逾時、CSS 模擬與列印引擎有落差，無法親自截圖確認頁數**；
  已請老師用 Ctrl+P 列印預覽確認，若仍溢頁可再把曲線降到 40/38mm。
- **sync 同週去重**：`sync-notion.mjs` 同座號＋同週次＋同型別只留「最新建立」列（rows 已依 _created 遞增，
  後者覆蓋）。避免稀疏舊列與完整新列同週各畫一次；成長曲線/週報只採完整版。屬防呆保險，對真實資料也安全。

**⭐ 成長曲線排序修正（2026-07-13，重要）**
- **問題**：`sync-notion.mjs` 原本 periods 依 `_created`（建立時間）排序，但週報若非按週次順序建立
  （本次 mock 即如此），期末總報告的「五向度逐週成長曲線」會鋸齒亂序（例：座號3 曾是 10→20→5→15）。
- **修正**：新增 `periodWeek()` 解析「第X週」（國字/阿拉伯數字 1–99），排序改為
  **期末總報告永遠最後 → 每週列依週次序號 → 同週退回建立時間**。已 commit＋push＋同步驗證。
- 影響全站所有學生（含未來真實資料），一勞永逸。

**✅ 班級銀行「能看也能買」——2026-07-18 已建成上線（原 Task 2 設計，改教師專區處理）**
- 新 Notion「🛒 兌換申請」DB（DS `f4c697c6-7c27-4d20-b54a-febac0fc5d64`）＝購買明細正本。
- 學生端 bank.html：登入後「🛒 我要兌換」（餘額不足→「還差 N 幣」；每人 3 筆待處理上限）。
- 教師端 teacher.html「🛒 兌換申請」：核可＝代理自動扣幣＋庫存−1＋留明細；駁回填原因；明細 50 筆＋Notion 完整查詢。
- 代理 v2.1（apps-script-proxy-v2.gs）：redeem_request 以座號＋查詢碼驗證、價格以 Notion 商店為準（不信任前端）。
- ⚠️ **老師還要做**：重新部署代理 v2.1（貼上檔案→部署新版本，5 分鐘）＋確認 integration 可存取四個 DB；
  部署後跑一次 E2E（兌換→核可→更新班網→存摺顯示消費）。

**⚠️ 注意事項（給下次）**
- `data/reports/1.json`、`30.json` 是舊「範本」加密報告，仍存舊等第（考試成績/A/良）。
  開學前重生或刪除；線上站範本會顯示舊標籤直到重新同步。
- Notion `update-data-source` 的 RENAME＋ALTER 同批會產生重複欄位；**改結構時一次一動作、逐步驗證**。
- 全流程模擬（27 人一學期）v3 已於 07-12 驗證通過並清庫；本次未再寫入正式庫（改採邏輯稽核）。

---

## 套件四：2026-07-18 session 變更摘要（兌換申請流＋Phase 3）

**A. 班級銀行兌換申請流**（詳見上方「能看也能買」段與 `docs/班級銀行制度設計.md` §七）
**B. Phase 3 教學五段流程**：🚀 教學單元 DB（DS `d28efc6b-3f34-4a97-b72b-ddeb5ec51147`）＋
`syncLessons()`→lessons.json＋cockpit.html（狀態分組、五段進度 ✓/○、連結鈕；noindex 零個資；
入口＝頁尾 🚀＋教師專區）＋skill `lesson-flow` 三件套＋路由 R12 開通＋快速鍵「🚀 開新單元」。
線上已驗證（示範單元列，開學前可刪）。
**C. 收件匣 8 筆測試任務**移至 Notion「🗑️ 收件匣測試任務封存」頁（老師確認後整頁刪除即可）。
**D. 工作台回流**：源碼不可得。
⚠️ **2026-07-19 更正**：原記「資料在老師自有 Firebase `classhubgame`」**有誤**——該 Firebase 專案
屬工作台作者「方方老師」（CLASSROOM HUB）所有，老師的資料只是掛在 `users/{uid}/` 底下。
故**不採直讀 Firebase**，理由三點：① 需長期保存 Google ID token，牴觸 AGENTS.md 鐵則 7
（憑證不進本資料夾）且 token 1 小時過期；② 讀取消耗的是作者頻寬（其檔名版本為
`rtdb-bandwidth-p3`，作者正在撙節頻寬成本）；③ 程式輪詢第三方服務有條款風險。
**改採方案 A-2：工作台內建匯出 → 本機解析 → class-log 入庫**（見下方套件五）。
**E. 本機 git 修復**：Drive 同步造成 main 無 commit＋殘留 main.lock → 已 reset origin/main 接回。

---

## 套件五：2026-07-19 工作台回流（方案 A-2 · 匯出檔解析）

**背景**：工作台源碼不可得；Firebase 專案屬作者所有（見套件四 D 更正）。
既有出口盤點（掃 `i18n/ui-zh-TW.js` 得知）：匯出本機資料（完整 JSON）／匯出 Excel（出勤）／
複製成績名單／複製未繳交作業報告。選用第一項，資料最完整且為結構化 JSON。

**工具**：`class-website/scripts/parse-workbench-export.mjs`（零相依，Node 18+）

```bash
node scripts/parse-workbench-export.mjs <匯出檔.json>   # 產生待審清單
node scripts/parse-workbench-export.mjs --commit        # class-log 寫入後才標記已處理
```

**設計要點（踩雷點都在這）**
1. **不採 `student.score`**：它是**累計值**，重跑必定重複計算。只取有穩定事件鍵的三種來源——
   代幣流水 `tokenLedger[].id`、出勤 `dateKey+studentId`、作業 `homeworkId+studentId`。
2. **兩段式把關**：解析只產 `data/workbench/pending.json` 待審批次，**不寫 Notion**；
   老師確認並由 class-log 入庫後，再跑 `--commit` 才更新 `processed.json`。
   中途放棄不會讓事件被永久跳過（符合把關三分類：批次變動停待審）。
3. **單一寫入口徑**：本腳本不自寫紀錄庫，產出一句話清單交 class-log（金幣／程度規則只維護一份）。
4. **座號對應＋交叉驗證**（老師 2026-07-19 確認：工作台 `student.id` 即座號、姓名與名冊一致）：
   以 **id 為主鍵**，再以**姓名交叉驗證**。不因前提成立就省略檢查——正因兩訊號本該一致，
   不一致時就是真實異常的最佳警報（名冊異動未同步／轉學生／匯錯班級）。
   三種處置：兩者相符 → 直接採用；座號在名冊但姓名不符 → 採用並標「座號待確認」附具體原因；
   兩者皆對不到 → 列需人工且事件**不進清單**。輸出**全程只有座號、無姓名**
   （連警告訊息也只說「對應到座號12」而不印姓名）。
5. **程度＝代幣絕對值**（2026-07-19 裁定後實作，原「不由代幣換算」已失效）：
   代幣事件 `±n` → class-log 句子 `±n`（程度 n）→ 現行金幣公式 ×5 入帳。
   出勤／作業維持中性、不帶程度也不入金幣。待審清單每筆並列換算後崑山幣供老師核對。

**崑山幣＝代幣 同步裁定（老師 2026-07-19）**
- **範圍＝只同步行為獎懲**：工作台記的點數自動流入銀行；薪水／利息／商店消費只存在銀行端。
  兩邊餘額**本來就不同**（工作台＝行為幣、銀行＝總資產），不做反向對帳，零額外人工。
- **為何不做全鏡像**：工作台是封閉第三方（作者的 Firebase），寫入端永遠只能老師手動；
  全鏡像等於每週手抄薪水利息進工作台，違反 CP 值原則。
- **匯率（建議值，實作開工前老師可改）**：1 代幣＝程度 1＝5 崑山幣。
  工作台 ±n → class-log 句子 ±n（程度 n）→ 現行公式 ×5 入帳，激勵尺度與既有獎懲金（±5）
  ／週薪（30）一致，金幣公式不用動。若改 1:1 需另開「直接指定金幣」通道，牽動 class-log。
- **✅ 已實作（2026-07-19）**：`parse-workbench-export.mjs` 的 `toSentence()` 已改 `±|delta|`
  （僅代幣來源；出勤／作業維持中性無金幣），待審清單加註「→ 崑山幣 ±n×5」，
  匯率抽為常數 `崑山幣匯率 = 5`（僅供預覽顯示；**真要改匯率須連同 class-log 的金幣公式一起改**，
  只改此常數會造成預覽與實際入帳不一致）。老師操作手冊該節與本套件第 5 點已同步更新。
  合成檔煙霧測試通過：±3／-2／+1 正確換算、0 值略過、出席與已完成不記錄、輸出零姓名。

**工作台資料結構**（自 `class-manager-data.step5.min.js` 逆推）
- 根：`{schema, revision, lastModifiedAt, currentClassId, classes[]}`
- 班級：`{id, name, students[], homeworks[], attendanceByDate{}, groups[], ...}`
- 學生：`{id, name, score, grade, tokens, tokenLedger[{id,delta,reason,at}], ...}`
- 出勤：`attendanceByDate[dateKey][studentId] = {status, note, ...}`；
  status ∈ present/late/absent/leave（**present 不回流**）
- 作業：`homeworks[].studentStatus[] = {id, status}`；
  status ∈ pending/submitted/needs_correction/completed（**只回流 pending／needs_correction**）

**驗證狀態**
- ✅ 合成測試檔實跑：座號對應、0 值略過、出席／已完成不記錄、對不到名冊者不混入、
  輸出零姓名、commit 後重跑 0 筆、追加事件只出新筆、竄改 `score` 不影響結果。
- ✅ 交叉驗證三種異常皆攔截：座號在名冊但姓名對到別的座號／姓名完全不在名冊／座號與姓名皆對不到
  （前二者標「座號待確認」仍入清單，第三者排除）。
- ✅ 全班 27 人正常案例回歸測試：零警告、27 筆、座號依數值（非字典序）排序。
- ⏳ **尚缺真實匯出檔的欄位煙霧測試**：座號語意已由老師確認，剩下的是匯出檔實際欄位名
  （根層 `classes`、`tokenLedger[].{id,delta,reason,at}`、`attendanceByDate`、`homeworks[].studentStatus`）
  ——這些是自壓縮後的 JS 逆推，可信但未經真實檔證實。開學首次使用時跑一次即可確認。
