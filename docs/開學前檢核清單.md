# ✅ 開學前檢核清單

> 開學日：**2026-08-31**。本清單彙整散落在各版本紀錄／交接筆記的「老師端待辦」，
> 一頁勾完即可安心開學。系統面的建置已完工，剩下的都是只有老師能做的部署與資料動作。
> 最後更新：2026-07-25。
>
> **2026-07-25 更新**：Phase F 建立 Notion API 引擎後，token 已實測具備寫入／刪除／改 schema 能力，
> 原本「只有老師能手動做」的兩項資料動作（清測試資料、下架模擬報告）**已由腳本代勞完成**。
> **§一必辦已全數結清**，開學前無待辦；§二為平時提醒，非開學前必做。

---

## 一、必辦（開學前務必完成）

- [x] ~~**清除 Phase A 驗收測試資料**~~ **✅ 2026-07-25 已由腳本代勞，老師不必手動刪**

  Phase F 建立的 Notion API 引擎已具備刪除能力，故改用 `f01-purge-test-data` 批次清除
  （Actions run `30161678423`）。實際結果：**封存 23 筆、失敗 0、回讀殘留 0**。

  | 資料庫 | 篩選條件 | 筆數 | 結果 |
  |---|---|---|---|
  | 📝 班經與學習紀錄庫 | 週次 = `115上第九週(10/26-10/30)` | 7 | ✅ 已清 |
  | 📝 班經與學習紀錄庫 | 日期 = 2026-07-24～07-25 | 2 | ✅ 已清 |
  | 📊 學生學習報告 | 期間 = `115上第九週(10/26-10/30)` | 3 | ✅ 已清 |
  | 📥 任務收件匣 | 全部 | 11 | ✅ 已清 |

  說明：封存＝進 Notion 垃圾桶，**30 天內可還原**（若發現誤刪，到 Notion 垃圾桶救回即可）。
  Drive 上那張成績單照片（`20260725-082023_image.jpg`）是你自己的檔案，要不要留由你決定。

- [x] ~~**確認站上模擬報告已清乾淨**~~ **✅ 2026-07-25 已由腳本下架**

  `f04-unpublish-mock-reports` 盤點出發布中 8 筆（**全部是座號 2**，與原記載一致），
  已全數取消「發布」勾選並回讀確認 0 筆發布中（Actions run `30162458331`）。
  班網同步後線上實測：`data/reports/index.json` 為 `[]`、`data/reports/2.json` 回 **404**，
  家長以查詢碼登入會顯示「查無已發布的報告」。

  報告資料本身仍留在 Notion，開學後要用時重新勾選「發布」即可。

---

## 二、每次使用前 / 平時要記得

- [ ] **記住「更新口令」**：設在 Apps Script 專案 → 專案設定 → 指令碼屬性 `PASSWORD`。
  忘記可到 [script.google.com](https://script.google.com) 重設。按「⚡ 立即更新班網」時要輸入。
- [ ] **工作台真實匯出檔煙霧測試**（開學首次回流時跑一次即可，非開學前必辦）：
  第一次用工作台「匯出本機資料」後，跑
  `node scripts/parse-workbench-export.mjs <匯出檔.json>`，
  確認欄位（`classes`／`tokenLedger`／`attendanceByDate`／`homeworks[].studentStatus`）都對得上。
  這些欄位名是從壓縮 JS 逆推的，可信但未經真實檔證實。

---

## 三、已完成（存查，不用再做）

- [x] **Apps Script 代理 v2.1 重新部署＋兌換流 E2E 實測**（2026-07-25 完成）
- [x] **Phase A 八項驗收全過＋雲端排程上線**（2026-07-25 完成）
  - 驗收：手機送單／拍照上傳成績單（18筆零誤差）／一鍵更新班網（Actions 確認）／
    錯誤口令被拒＋鎖10分／R01入庫／R00失敗白話／R12建待辦／週結公式反推驗證三項全吻合。
  - 排程：claude.ai routines 兩條（台北平日 07:00 與 08:00–17:00，**每小時**一輪）已啟用並實跑通過。
  - ⚠️ **與原設計的差異**：平台最小間隔 1 小時，故非原訂「每 30 分」；
    教師專區文案若要精確，可把「約 30 分內處理」改為「約 1 小時內處理」。
  - ⚠️ 排程 v1 只自動跑「記錄類／查餘額／更新班網」，週結·報告·成績等一律停待審不自動產出，
    需要時在 Claude Code 對話說一聲即可。
- [x] Notion 名冊真實幹部職務／週薪／職務說明指派（2026-07-24 完成）
- [x] 班級公約圖片上傳（2026-07-24 完成）
- [x] 學生查詢碼分發（2026-07-24 完成）
  - ⚠️ 提醒：查詢碼既已分發，**別再把（模擬）報告放到真實在學座號並發布**，
    以免家長登入看到假資料。測試請用完即下架（見 §一第三點）。
- [x] 期末總報告功能、報告「理財表現」區塊（2026-07-13）
- [x] 一鍵「⚡ 立即更新班網」按鈕與更新代理（2026-07-13 實測通過）
- [x] 班級商店去重、內容評量改版、四等第統一（2026-07-13）

---

## §代理 v2.1 說明（為什麼「重新部署」是必辦）

班網的兌換與一鍵更新，都是前端呼叫**同一支 Google Apps Script 代理**
（網址存在 `data/site-config.json` 的 `updateProxyUrl`；前端見 `assets/js/bank.js`、`teacher.js`）。

- 這支代理**先前部署的是 v2**，只會處理「一鍵更新班網」（`sync-now`）——這部分 2026-07-13 已實測正常。
- 2026-07-18 為了「班級銀行能看也能買」，把代理升級到 **v2.1**，**新增了兌換動作**
  （`redeem_request`：學生申請、老師核可自動扣幣扣庫存）。
- 但 Apps Script 的規則是：**改了程式碼，必須手動「部署新版本」，線上跑的才會更新**。
  Google 端目前很可能仍是舊的 v2（沒有兌換處理），所以在老師重新部署前，
  **兌換按鈕會失敗或無回應**——這就是「兌換 E2E 未確認」的意思。
- 一鍵更新不受影響（v2 就有），只有兌換要等重新部署。5 分鐘的動作，做完跑一次上面的 E2E 就結案。
